<UserControl 
    x:Class="PlantMonitorGateway.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:ViewModels="clr-namespace:PlantMonitorGateway.ViewModels"
    mc:Ignorable="d" 
    d:DesignHeight="350"
    d:DesignWidth="300"
    d:DataContext="{d:DesignInstance ViewModels:MainViewModel}">

    <UserControl.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding ConvertTextCommand}"/>
    </UserControl.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label   Grid.Row="0" Foreground="Blue" Margin="5,0,5,0">Text To Convert</Label>
        <TextBox Grid.Row="1" Text="{Binding SomeText, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
        <Label   Grid.Row="2" Foreground="Blue" Margin="5,0,5,0">History</Label>
        <ListBox Grid.Row="3" ItemsSource="{Binding History}" Margin="5"/>
        <Button  Grid.Row="4" Command="{Binding ConvertTextCommand}" Margin="5">Convert</Button>
    </Grid>
</UserControl>
